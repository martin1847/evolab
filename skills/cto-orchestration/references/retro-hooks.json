{
  "//": "复盘仪式压缩点提醒 wiring 真源（可选——编排位席位建议接入；提醒不阻断）。接入=command 前缀替换为本 skill 安装根绝对路径（hooks 不展开 ~、不加解释器前缀——脚本自带 shebang）。接入层级判据：编排位常驻的机器接用户级 ~/.claude/settings.json（复盘仪式是席位属性、不是项目属性，逐项目接会漏新项目——多席位机器实证 2026-07-28）；仅少数项目做编排时才接项目级，两级都接会重复注入。接线是全局的，所以脚本自己认身份：只在 payload cwd 所在仓正在被编排（同仓有 LIVE 席位或今/昨相位账本 start 行，同仓 = git common dir）时输出，其余一个字不输出——与 E1 / Stop 门共用 agentctl/identity.py 的同一谓词。PreCompact 只能给用户可见 systemMessage（harness schema 不收 additionalContext，错误形态会被整体拒收、静默失败——2026-07-28 实证）；模型上下文注入靠压缩后 SessionStart，脚本自判 source=compact、其余静默。UserPromptSubmit 触点（session-economics.py）只在被编排仓、且只在上下文越过 300k/450k/600k 档或闲置过缓存 1h 边界时各说一行 additionalContext（每档每会话一次、同一次闲置一次），其余每一条 prompt 零输出、零 git、零 identity 调用；它只提醒收口即 /compact（读数只算最后一个压缩边界之后那段——压缩后第一条 prompt 判不出就不说，不误报压缩前那个读数），不拦、不改状态、从不非零退出。同一个 retro-reminder.sh 还管 SessionStart source=startup|clear 的**开场指针**（零接线改动）：谓词只有「payload cwd 所在 git 仓根下有 docs/ACTIVE_CONTEXT.md」一条，不叠加被编排谓词（换会话后往往还没有 LIVE 席位，叠加会恰好在最需要接上时静默）；输出一行「快照在哪 + Last rewritten 日期」，不内联正文，判不出即零字节。",
  "PreCompact": [
    { "matcher": "", "command": "references/retro-reminder.sh" }
  ],
  "SessionStart": [
    { "matcher": "", "command": "references/retro-reminder.sh" }
  ],
  "UserPromptSubmit": [
    { "matcher": "", "command": "references/session-economics.py" }
  ]
}

{
  "//": "agent-mail wiring 真源（canonical entries）。接入=把 command 换成本 skill 安装根的绝对路径（hooks 不展开 ~、不加 python3 前缀——脚本自带 shebang 直接 exec），并进项目 settings（CC .claude/settings.json；Codex/omp 见 SKILL 接入节说明）。两个事件同一脚本：SessionStart=开场全量冒泡；UserPromptSubmit=长跑 session 的增量投递（只报新到、报过不复读、无新静默——不重启也能收到中途来信）。",
  "SessionStart": [
    { "command": "mail-check.py" }
  ],
  "UserPromptSubmit": [
    { "command": "mail-check.py" }
  ]
}

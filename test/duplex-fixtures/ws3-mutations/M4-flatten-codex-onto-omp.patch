--- a/skills/cto-orchestration/references/agentctl/duplexctl.py
+++ b/skills/cto-orchestration/references/agentctl/duplexctl.py
@@ -2392,6 +2392,9 @@
         },
     },
 }
+# MUTATION: codex is handed omp's contract wholesale — one flat "common" map.
+PROVIDERS["codex"]["capabilities"] = dict(PROVIDERS["omp"]["capabilities"])
+
 # ── everything below is DERIVED from the table above ─────────────────────────
 CAPABILITIES = {name: rec["capabilities"] for name, rec in PROVIDERS.items()}
 # route id → the branch that really performs it. Send routes (frame builder / JSON-RPC

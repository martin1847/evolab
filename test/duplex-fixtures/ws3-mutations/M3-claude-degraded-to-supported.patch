--- a/skills/cto-orchestration/references/agentctl/duplexctl.py
+++ b/skills/cto-orchestration/references/agentctl/duplexctl.py
@@ -2312,12 +2312,7 @@
             # frame, so it lands at the turn BOUNDARY. THE degradation this contract exists
             # to stop advertising as native steering. The protocol has no per-frame ack, so
             # `send` reports delivery, never acceptance.
-            "steer": _cap(
-                DEGRADED, route="user", impl=build_frame,
-                note="claude has no public mid-turn frame — while a turn is RUNNING the "
-                     "steer is delivered as a QUEUED message that lands at the next turn "
-                     "boundary, NOT inside the running turn (idle: it opens the next turn "
-                     "immediately)"),
+            "steer": _cap(SUPPORTED, route="user", impl=build_frame),
             "interruptTurn": _cap(
                 UNSUPPORTED,
                 refusal="claude has no interrupt/replace frame — `agentctl stop` the "

--- a/skills/cto-orchestration/references/agentctl/duplexctl.py
+++ b/skills/cto-orchestration/references/agentctl/duplexctl.py
@@ -2319,7 +2319,7 @@
                      "boundary, NOT inside the running turn (idle: it opens the next turn "
                      "immediately)"),
             "interruptTurn": _cap(
-                UNSUPPORTED,
+                SUPPORTED,
                 refusal="claude has no interrupt/replace frame — `agentctl stop` the "
                         "session, then restart with `--resume <session_id>` (engine args) "
                         "and the new goal"),

--- a/skills/cto-orchestration/references/agentctl/duplexctl.py
+++ b/skills/cto-orchestration/references/agentctl/duplexctl.py
@@ -571,7 +571,7 @@
         elif ftype in clears:
             pending = None
     if pending is not None:
-        return "WAITING", clip(json.dumps(pending, ensure_ascii=False), 200)
+        return "IDLE", clip(json.dumps(pending, ensure_ascii=False), 200)
     return "IDLE", "isStreaming=false, queue empty"
 
 

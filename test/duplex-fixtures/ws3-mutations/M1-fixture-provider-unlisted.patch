--- a/skills/cto-orchestration/references/agentctl/duplexctl.py
+++ b/skills/cto-orchestration/references/agentctl/duplexctl.py
@@ -2401,6 +2401,10 @@
           for name, caps in CAPABILITIES.items()}
 # engine → state projector, consumed by classify.
 PROJECTORS = {name: rec["projector"] for name, rec in PROVIDERS.items()}
+# MUTATION: a fixture adapter is hand-added to the DERIVED registries — the lane can
+# route and project it, but no capability cell declares it.
+ROUTES["fixtureprov"] = {"user": build_frame}
+PROJECTORS["fixtureprov"] = project_claude
 
 
 # The watch lane's published contract, same spirit as the capability table: ONE table, read by
